import { KNOWLEDGE_BASE_URL } from 'src/config-global'

const BASE_URL = KNOWLEDGE_BASE_URL

export const KNOWLEDGE_BASE_MAP: Record<string, string> = {
  '/dashboard/app': `${BASE_URL}/posts/getting-started`
}
